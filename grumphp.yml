parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
      phplint: ~
      phpcsfixer:
                  config_file: ~
                  config: ~
                  fixers: []
                  level: ~
                  verbose: true
